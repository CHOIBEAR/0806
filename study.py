# 📘 FastAPI 구조 복습 퀴즈 10문제 🧠  
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q1. FastAPI에서 모듈화된 라우팅을 위해 사용하는 클래스는?
# ① FastRouter  
# ② APIEndpoint  
# ③ APIRouter   o
# ④ RouteHandler

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q2. 아래 중 FastAPI 앱 설정에 사용되는 키가 아닌 것은?
# ① title  
# ② version  
# ③ path  o
# ④ docs_url 

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q3. routers.py에서 ctr.add_api_route(**item)으로 등록되는 정보가 아닌 것은?
# ① methods  
# ② path  
# ③ tags  o
# ④ endpoint

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q4. urls.py의 urls 리스트는 무엇을 담고 있나요?
# ① FastAPI 앱 인스턴스  
# ② 라우팅 설정 딕셔너리들의 리스트  o
# ③ Swagger 문서  
# ④ 요청 데이터 모델

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q5. routers.py의 api_config 딕셔너리를 사용하는 곳은 어디인가요?
# ① urls.py  
# ② APIRouter  
# ③ main.py  o
# ④ endpoint 함수 내부

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q6. include_router 함수에 사용되는 키워드 인자가 아닌 것은?
# ① prefix  
# ② tags  
# ③ router  
# ④ method o

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q7. docs_url="/api_docs"로 설정하면 어떤 효과가 있나요?
# ① 문서가 비활성화된다  
# ② /api_docs 경로로 Swagger 문서가 제공된다  o
# ③ redoc 문서가 활성화된다  
# ④ API 경로가 전부 /api_docs 아래로 들어간다

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q8. routers.ctrs에 저장된 각 항목은 어떤 자료형인가요?
# ① 문자열  
# ② 라우터 객체만 저장됨  
# ③ prefix, tags, router가 담긴 딕셔너리  o
# ④ pydantic 모델

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q9. FastAPI에서 엔드포인트 함수의 기본 반환형은?
# ① HTML  
# ② JSON  o
# ③ XML  
# ④ Binary

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ❓ Q10. 기능별 라우터를 분리하면 어떤 장점이 있나요?
# ① 성능이 빨라진다  
# ② URL이 자동으로 변한다  
# ③ 코드 유지보수 및 확장이 쉬워진다  o
# ④ 데이터베이스가 필요 없다

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
